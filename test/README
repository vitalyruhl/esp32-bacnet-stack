This directory contains PlatformIO Unity tests for the ESP32 BACnet Stack.

Local compile-only test run:

  pio test -e usb --without-uploading --without-testing

Hardware upload and monitor commands are intentionally outside the default
validation flow.
