# Repository Settings

Current GitHub repository:

- URL: <https://github.com/vitalyruhl/esp32-bacnet-stack>
- Visibility: private
- Issues: enabled
- Wiki: disabled
- Discussions: not enabled
- GitHub Project: `ESP32 BACnet Stack` (#6), private

## Security

- Dependabot alerts: enabled by repository API.
- Dependabot security updates: enabled by repository API.
- Dependabot version updates: configured for GitHub Actions in
  `.github/dependabot.yml`.
- GitHub Advanced Security and secret scanning could not be enabled for this
  private personal repository because GitHub reported that Advanced Security has
  not been purchased.

## Branch Protection

The remote repository currently has `feature/initial-bacnet-stack-setup` as its
default branch because only the feature branch was pushed during private setup.
No `main` branch was pushed or merged as part of setup.

TODO: After the first reviewed integration creates or updates `main`, configure
branch protection or a ruleset for `main` with the PlatformIO workflow as a
required status check if the account/repository plan supports it.
