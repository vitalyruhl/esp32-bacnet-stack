# BACnet Agent Start Points

- Status: curated navigation summary only.
- This memory is orientation only and does not replace repository files, user instructions, `.github/AGENTS.md`, or `.github/agents/project.agent.md`.

## Mandatory First Reads

- Read `.github/AGENTS.md` when a full governance reload is required.
- Read `.github/agents/project.agent.md` for repo-specific validation and risk context.
- Read `.github/agents/workflow.agent.md` for branch, PR, merge, and issue/project status work.
- Read `.github/agents/refactor.agent.md` before product-code changes.

## BACnet / Serena Entry Points

- Read `mem:shared/project_overview` for repo role and scope.
- Read `mem:shared/build_and_test_commands` before validation.
- Read `mem:shared/esp32_platformio_notes` before PlatformIO or hardware-facing work.
- Read `mem:shared/bacnet_reference_landscape` before using upstream/reference repositories.
- Read `mem:shared/bacnet_transport_support_matrix` before transport assumptions.
- Read `mem:shared/reusable_stack_architecture_lessons` before changing client/server boundaries.
- Read `mem:shared/non_reusable_commercial_references` and `mem:shared/bacnet_gateway_example_patterns` before borrowing ideas from non-core examples.
- Read `mem:shared/bacnet_open_questions` before turning uncertain reference facts into implementation assumptions.

## Non-Assumptions

- Do not assume current-repo MS/TP support.
- Do not assume upstream/reference build commands are current-repo validation defaults.
- Do not assume proprietary or gateway examples are reusable implementation sources.
- Do not skip mandatory direct governance reads because a Serena memory exists.

## Primary Sources

- `.github/AGENTS.md`, `.github/agents/project.agent.md`, `.github/agents/workflow.agent.md`, `.github/agents/refactor.agent.md`
